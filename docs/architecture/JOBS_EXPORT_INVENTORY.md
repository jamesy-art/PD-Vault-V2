# Jobs Export Inventory

**Date:** 2026-08-07  
**Audit only** — every Jobs-related export entry point and helper.

---

## 1. Canonical — Track A8 Export Workspace

### CLI entry points

| Path | Role | Category |
|------|------|----------|
| `scripts/export/export.js` | Production export / `--preview` | **Canonical** |
| `scripts/export/preview.js` | Preview only | **Canonical** |
| `scripts/export/list.js` | Browse Ready / `--history` | **Canonical** |
| `scripts/export/smoke-test.js` | Regression smoke | **Canonical** |
| `scripts/export/README.md` | Operator docs | **Canonical** |

### Library

| Path | Role | Category |
|------|------|----------|
| `scripts/lib/export/index.js` | `ExportWorkspace` orchestration | **Canonical** |
| `scripts/lib/export/readyCatalog.js` | List Ready docs | **Canonical** |
| `scripts/lib/export/preview.js` | Mode selection + preview DTO | **Canonical** |
| `scripts/lib/export/exportValidator.js` | Fail-closed validation | **Canonical** |
| `scripts/lib/export/ExportMapping.js` | Base mapping class | **Canonical** |
| `scripts/lib/export/mappings/JobsMapping.js` | Jobs CSV contract + `mapRow` | **Canonical** |
| `scripts/lib/export/MappingRegistry.js` | Registers JobsMapping (+ stubs) | **Canonical** |
| `scripts/lib/export/csv.js` | `encodeCsv` / escape / sha256 | **Canonical** |
| `scripts/lib/export/csvGenerator.js` | Per-type CSV + package identity | **Canonical** |
| `scripts/lib/export/packageBuilder.js` | Writes package + report mirrors | **Canonical** |
| `scripts/lib/export/manifest.js` | `manifest.json` builder | **Canonical** |
| `scripts/lib/export/packageRegistry.js` | History / registry paths | **Canonical** |
| `scripts/lib/export/ExportReport.js` | Report DTO + markdown | **Canonical** |
| `scripts/lib/export/sectionExtract.js` | Body `##` section extract | **Canonical** |
| `scripts/lib/export/types.js` | Export version constants | **Canonical** |

### Ready dependency (not Export, but required)

| Path | Role | Category |
|------|------|----------|
| `scripts/lib/ready/ReadyWriter.js` | `readyRootFor` → `content/jobs/ready/` | **Canonical runtime** |
| `scripts/ready/promote.js` | Creates Ready packages | **Canonical** (upstream) |

---

## 2. Experimental / stubs (same registry, not Jobs production)

| Path | Role | Category |
|------|------|----------|
| `scripts/lib/export/mappings/SimpleTypeMapping.js` | Generic mapping for news, interviews, collections, events, products, company_updates | **Experimental stub** |

Registered in `createDefaultMappingRegistry()` but not the Jobs production path.

---

## 3. Not Jobs package export (other domains)

| Path | Role | Category |
|------|------|----------|
| `scripts/lib/companyCsvExport/*` | Company CSV export | Canonical **Companies** |
| `scripts/lib/typeCsvExport/*` | Types CSV export | Canonical **Types** |
| Designer CSV exporters (if present under scripts) | Designers domain | Separate |

These do **not** write Track A8 `exports/packages/*/jobs.csv`.

---

## 4. Laravel (consumer / parallel)

| Path | Role | Category |
|------|------|----------|
| `backend-laravel/.../ImportExportPackageCommand.php` | `php artisan import:package` | **Canonical consumer** |
| `.../Importers/JobsImporter.php` | Maps Vault `jobs.csv` | **Canonical consumer** |
| `.../Imports/JobCsvImporter.php` | Admin Filament CSV | **Parallel / legacy admin** |
| `.../JobsCsvImportService.php` | Minimal legacy CSV | **Legacy** |

---

## 5. Dead / stray relative to export

| Item | Notes | Category |
|------|-------|----------|
| `JobsExporter` class | **Does not exist** | N/A |
| Second Vault writer of Jobs export packages | **Not found** | N/A |
| `wiki/jobs/_ready/` | Documented in `vaultPaths.js`; unused by ReadyWriter/export | **Doc drift / unused** |
| Loose `wiki/jobs/ready-clip-*` dirs | Not scanned by `readyCatalog` | **Stray** |

---

## 6. Live package inventory (validation artifact)

```
exports/packages/pkg-64af29096d1ce0d2/
  jobs.csv                 ← generated by JobsMapping + encodeCsv
  manifest.json
  export_report.json
  export_report.md
```

Also: `exports/reports/pkg-64af29096d1ce0d2.{json,md}`

---

## 7. Ownership summary

| Concern | Component |
|---------|-----------|
| CSV generation | `JobsMapping` + `csvGenerator` / `csv.js` |
| Package generation | `packageBuilder.writeExportPackage` |
| Asset packaging | **None** on Jobs export |
| Reports | `ExportReport` written by `packageBuilder`; mirrors via `packageRegistry` |
| Validation | `exportValidator` inside `ExportWorkspace.export` |
| CLI | `scripts/export/export.js` |

---

## 8. Consolidation note (audit only)

No Vault Jobs exporter consolidation is required: there is a **single** canonical Ready → package implementation (Track A8).

Ops caution only: do not confuse Vault packages with Admin `JobCsvImporter` uploads.
